
const expo = (base, exponent) => { 
    let num = Math.pow(base, exponent);//
    console.log(Math.pow(base, exponent));
    return num
};

export default expo;

// const expo = (base, exponent) => { 
//     let num = (base ** exponent);//
//     console.log(Math.pow(base, exponent));
//     return num
// }

// export default expo;
